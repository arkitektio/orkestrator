overwrite: true
schema: "http://jhnnsrs-lab/dokuments/graphql"
documents: "graphql/dokuments/**/*.graphql"
generates:
  src/renderer/src/dokuments/api/fragments.ts:
    plugins:
      - "fragment-matcher"
  src/renderer/src/dokuments/api/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      withHooks: true
      reactApolloVersion: 3
      apolloReactHooksImportFrom: "@/lib/dokuments/funcs"
      # Reference fragment types by name instead of inlining them: deep fragments
      # otherwise blow graphql.ts up to many MB and stall tsc.
      inlineFragmentTypes: combine
  src/renderer/src/dokuments/graphql.schema.json:
    plugins:
      - "introspection"

  graphql/schemas/dokuments.graphql:
    plugins:
      - schema-ast
