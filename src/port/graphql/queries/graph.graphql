query DetailWhale($id: ID!) {
	whale(id: $id) {
		...DetailWhale
	}
}

query Whales {
	whales {
		...ListWhale
	}
}

query GithubRepos {
	githubRepos {
		...ListGithubRepo
	}
}

query DetailGithubRepo($id: ID!) {
	githubRepo(id: $id) {
		...DetailGithubRepo
	}
}
