import React, { useState, useEffect } from "react";
import { DetailReservationFragment } from "../../arkitekt/api/graphql";

export interface FlussReservationProps {
  res: DetailReservationFragment;
}

export const FlussReservation: React.FC<FlussReservationProps> = (props) => {
  return <></>;
};
