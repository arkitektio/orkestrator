fragment DetailDeployment on Deployment {
  id
  identifier
  version
  scopes
  image
  requirements
  command
}

fragment ListDeployment on Deployment {
  id
  identifier
  version
  scopes
  image
  whales {
    id
  }
  command
}
