fragment DetailWhale on Whale {
  id
  config
  runtime
  createdAt
  clientId
  clientSecret
  scopes
  image
  pulled
  latestPull
  containers {
    id
    name
    image {
      ...DetailImage
    }
    logs
    labels
    status
  }
}

fragment ListWhale on Whale {
  id
  config
  runtime
  createdAt
  clientId
  clientSecret
  image
  pulled
  latestPull
}
