fragment DetailRepoScan on RepoScan {
  id
  identifier
  version
  scopes
  image
  runtime
  command
}

fragment ListRepoScan on RepoScan {
  id
  identifier
  version
  scopes
  image
  runtime
  whales {
    id
  }
  command
}
