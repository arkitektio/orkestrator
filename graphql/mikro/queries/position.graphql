query DetailPosition($id: ID!) {
  position(id: $id) {
    ...Position
  }
}

query PositionSearch($search: String, $values: [ID!]) {
  options: positions(
    filters: { search: $search, ids: $values }
    pagination: { limit: 30 }
  ) {
    value: id
    label: name
  }
}
