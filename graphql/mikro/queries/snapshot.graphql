query DetailSnapshot($id: ID!) {
  snapshot(id: $id) {
    ...Snapshot
  }
}

query Snapshots($limit: Int, $offset: Int) {
  snapshots(pagination: { limit: $limit, offset: $offset }) {
    ...ListSnapshot
  }
}
