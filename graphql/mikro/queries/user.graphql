query UserOptions($search: String, $values: [ID]) {
  options: users(
    filters: { search: $search, ids: $values }
    pagination: { limit: 30 }
  ) {
    value: id
    label: name
  }
}

query MikroUser($id: ID!) {
  user(id: $id) {
    sub
    id
    firstName
  }
}
