query GetChannel($id: ID!) {
  channel(id: $id) {
    ...Channel
  }
}

query SearchChannels($search: String, $values: [ID]) {
  options: channels(
    filters: { name: $search, ids: $values }
    pagination: { limit: 30 }
  ) {
    value: id
    label: name
  }
}
