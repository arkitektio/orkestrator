mutation PinRoi($id: ID!, $pin: Boolean!) {
  pinROI(id: $id, pin: $pin) {
    id
    pinnedBy {
      id
      email
    }
    pinned
  }
}

mutation DeleteRoi($id: ID!) {
  deleteROI(id: $id) {
    id
  }
}
