query Flow($id: ID!) {
  flow(id: $id) {
    ...Flow
  }
}

query Flows(
  $filters: FlowFilter
  $pagination: OffsetPaginationInput
  $ordering: [FlowOrder!]
) {
  flows(filters: $filters, pagination: $pagination, ordering: $ordering) {
    ...ListFlow
  }
}
