subscription TemplatesEvent($templateParams: [TemplateParamInput]) {
  templates(templateParams: $templateParams) {
    updated {
      ...ReservableTemplate
    }
    created {
      ...ReservableTemplate
    }
    deleted
  }
}
