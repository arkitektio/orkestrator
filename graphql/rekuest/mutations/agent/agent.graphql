mutation template(
  $interface: String!
  $definition: DefinitionInput!
  $instanceId: ID!
) {
  createTemplate(
    interface: $interface
    definition: $definition
    instanceId: $instanceId
  ) {
    id
  }
}
