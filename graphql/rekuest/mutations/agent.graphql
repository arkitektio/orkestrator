mutation DeleteAgent($id: ID!) {
  deleteAgent(id: $id) {
    id
  }
}

mutation KickAgent($id: ID!) {
  kickAgent(id: $id) {
    id
  }
}

mutation BounceAgent($id: ID!) {
  bounceAgent(id: $id) {
    id
  }
}
