fragment Release on Release {
  id
  name
  version
  app {
    identifier
  }
  flavours {
    ...ListFlavour
  }
  scopes
}

fragment ListRelease on Release {
  id
  version
  app {
    identifier
  }
  scopes
  flavours {
    ...ListFlavour
  }
}
