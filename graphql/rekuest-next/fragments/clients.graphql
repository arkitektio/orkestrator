fragment Client on Client {
  id
  clientId
}

# `Client.name` is gone server-side; the release (app + version) is what labels
# a client in pickers now.
fragment ListClient on Client {
  id
  clientId
  release {
    id
    version
    app {
      id
      identifier
    }
  }
}
