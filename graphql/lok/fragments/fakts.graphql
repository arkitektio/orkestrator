fragment PrivateFakt on FaktApplication {
  id
  clientId
  clientSecret
  scopes
  app {
    version
    identifier
  }
  token
}

fragment PublicFakt on FaktApplication {
  id
  clientId
  clientSecret
  scopes
  app {
    version
    identifier
  }
}
