fragment PrivateFakt on FaktApplication {
  id
  clientId
  clientSecret
  scopes
  app {
    version
    identifier
  }
}

fragment PublicFakt on FaktApplication {
  id
  clientId
  clientSecret
  scopes
  app {
    version
    identifier
  }
}
