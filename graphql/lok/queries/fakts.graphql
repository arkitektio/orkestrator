query PublicFakts {
  publicfaktapps {
    ...PublicFakt
  }
}

query PublicFakt($id: ID!) {
  publicfaktapp(id: $id) {
    ...PublicFakt
  }
}

query PrivateFakts {
  privatefaktapps {
    ...PrivateFakt
  }
}

query PrivateFakt($id: ID!) {
  privatefaktapp(id: $id) {
    ...PrivateFakt
  }
}
