query DetailSimulation($id: ID!) {
  simulation(id: $id) {
    ...DetailSimulation
  }
}

query ListSimulations(
  $pagination: OffsetPaginationInput
  $filters: SimulationFilter
  $ordering: [SimulationOrder!]
) {
  simulations(pagination: $pagination, filters: $filters, ordering: $ordering) {
    ...ListSimulation
  }
}

# Just the run's clock: what "Open on timeline" stages an experiment over.
query SimulationClock($id: ID!) {
  simulation(id: $id) {
    id
    name
    clock {
      id
    }
  }
}
